# Configuration avec MÊME PORT (pour tester l'erreur)
server {
    listen 8080;
    server_name localhost;
    root ./www;
    index index.html;
}

server {
    listen 8080;  # MÊME PORT - doit échouer
    server_name example.com;
    root ./www2;
    index index.html;
}
